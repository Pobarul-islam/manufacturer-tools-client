import React from 'react';

const Loading = () => {
    return (
        <div className='h-screen flex justify-center items-center'>
            <button class="btn loading bg-indigo-500">loading</button>
        </div>
    );
};

export default Loading;